config:
  security.nesting: "true"
description: Perfil para contenedores que ejecutan Docker
devices:
  eth0:
    name: eth0
    network: segbr0
    type: nic
  root:
    path: /
    pool: default
    type: disk
name: docker-host
